@startuml
title 测试/发布智能体活动图

|用户|
start
:进入用户智能体页面;
:选择目标智能体;
:选择操作类型;

if (选择测试?) then (是)
  |前端|
  :提交测试请求\n(工作流定义或智能体ID、输入参数);

  |后端|
  :校验用户身份与智能体管理权限;

  if (按工作流定义测试?) then (是)
    :解析临时工作流定义;
  else (否)
    :读取用户智能体定义;
    :读取绑定工作流定义;
  endif

  :加载当前用户可用工具;
  :调用工作流执行器执行测试流程;
  :收集节点结果、执行状态与最终输出;

  |前端|
  :返回测试结果;
  |用户|
  :查看测试输出与执行状态;

else (否)
  if (选择发布?) then (是)
    |前端|
    :提交发布请求\n(智能体ID);

    |后端|
    :校验用户身份与智能体管理权限;
    :读取用户智能体定义;
    :读取绑定工作流定义;
    :收集系统工具与用户工具;
    :组装代码生成所需工具集合;
    :调用代码生成器生成智能体代码;
    :更新发布信息与代码路径;

    if (进程管理器可用?) then (是)
      :停止旧版本智能体进程;
      :编译生成后的智能体代码;
      :启动新版本智能体进程;
      :回写端口、进程号与最新状态;
    else (否)
      :仅保存发布态信息;
    endif

    |前端|
    :返回发布结果;
    |用户|
    :查看发布状态与运行结果;
  endif
endif

stop
@enduml