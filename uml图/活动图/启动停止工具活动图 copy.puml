@startuml
skinparam defaultFontSize 30
skinparam titleFontSize 30
title 工具启动/停止活动图

|用户|
start
:进入工具管理页面;
:选择启动或停止操作;

if (选择启动工具?) then (是)
  :选择待启动工具;

  |前端|
  :封装启动请求;
  :提交工具启动请求;

  |后端|
  :校验用户身份与工具管理权限;
  :读取工具配置与运行模式;
  :判断工具是否支持启动;

  if (工具支持启动?) then (是)
    :启动本地 MCP 工具进程;
    :更新工具运行状态;
    |前端|
    :返回启动成功结果;
    |用户|
    :查看工具运行状态;
  else (否)
    |前端|
    :返回无需启动或启动失败结果;
    |用户|
    :查看提示信息;
  endif

else (否)
  :选择待停止工具;

  |前端|
  :封装停止请求;
  :提交工具停止请求;

  |后端|
  :校验用户身份与工具管理权限;
  :读取工具运行状态;

  if (工具正在运行?) then (是)
    :停止对应 MCP 工具进程;
    :更新工具状态为已停止;
    |前端|
    :返回停止成功结果;
    |用户|
    :查看最新工具状态;
  else (否)
    |前端|
    :返回无需停止结果;
    |用户|
    :查看提示信息;
  endif
endif

stop
@enduml