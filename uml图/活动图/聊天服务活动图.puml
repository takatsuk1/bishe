@startuml

skinparam defaultFontSize 30
skinparam titleFontSize 30
title 聊天功能活动图

|用户|
start
:进入聊天页面;
:输入聊天内容并发送请求;

|前端|
:封装消息、会话ID、用户令牌;
:提交聊天请求;

|后端|
:校验用户身份;
if (身份校验通过?) then (是)
  :读取历史会话上下文;
  :组织当前请求与上下文;
  :转发至后端智能体服务;

  |后端智能体服务|
  :分析用户问题;
  :调用大模型或工具处理任务;
  if (需要长时间执行?) then (是)
    :持续生成阶段性结果;
    |前端|
    :流式接收增量内容;
    :动态刷新聊天界面;
    |后端智能体服务|
    :继续执行并生成最终结果;
  else (否)
    :直接生成最终结果;
  endif

  |后端|
  :写入会话消息与处理结果;
  :返回最终结果;

  |前端|
  :展示聊天结果;
  |用户|
  :查看回答内容;

else (否)
  |前端|
  :返回身份校验失败信息;
  |用户|
  :重新登录或重新发起请求;
endif

stop
@enduml