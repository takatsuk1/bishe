@startuml
title 创建、编辑、保存智能体工作流活动图

|用户|
start
:进入工作流页面;
:点击新建工作流;

|前端|
:初始化空白工作流模型;
:生成或持有 workflowId;
:进入可视化编辑状态;

|用户|
:新增节点、调整边关系、修改节点参数;

while (是否继续编辑?) is (是)
  |前端|
  :更新当前工作流定义;
  :提交草稿保存请求\nPUT /workflows/{id};

  |后端|
  :校验用户身份与工作流管理权限;
  :解析工作流定义;
  :规范化节点与边配置;
  :检查工作流归属与草稿合法性;
  :将草稿保存到 Redis;

  |前端|
  :刷新当前画布状态;
  |用户|
  :继续编辑工作流;
endwhile (否)

|用户|
:点击保存工作流;

|前端|
:提交正式保存请求\n/user-workflows/save;

|后端|
:校验用户身份与工作流管理权限;
:解析保存请求;
:校验工作流名称、开始节点、节点集合与边关系;
if (工作流定义完整且合法?) then (是)
  :生成或确认 workflowId;
  :组装正式工作流定义;
  :写入 MySQL 数据库;
  :删除对应 Redis 草稿;
  |前端|
  :返回保存成功结果;
  |用户|
  :查看保存结果;
else (否)
  |前端|
  :返回保存失败信息;
  |用户|
  :继续修改工作流;
endif

stop
@enduml