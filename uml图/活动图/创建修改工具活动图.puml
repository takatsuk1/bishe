@startuml
title 工具创建/修改活动图

|用户|
start
:进入工具管理页面;
:选择创建或修改操作;

if (选择创建工具?) then (是)
  :填写工具标识、名称、类型、参数与配置;

  |前端|
  :封装创建请求;
  :提交工具创建请求;

  |后端|
  :校验用户身份与工具管理权限;
  :校验 tool_id、参数与配置合法性;
  :检查工具标识是否重复;

  if (校验通过?) then (是)
    :写入新工具配置;
    |前端|
    :返回创建成功结果;
    |用户|
    :查看新工具信息;
  else (否)
    |前端|
    :返回创建失败信息;
    |用户|
    :重新填写工具配置;
  endif

else (否)
  :选择已有工具并修改配置;

  |前端|
  :封装修改请求;
  :提交工具修改请求;

  |后端|
  :校验用户身份与工具管理权限;
  :读取原工具配置;
  :校验工具归属、参数与配置合法性;

  if (校验通过?) then (是)
    :更新工具配置;
    |前端|
    :返回修改成功结果;
    |用户|
    :查看最新工具信息;
  else (否)
    |前端|
    :返回修改失败信息;
    |用户|
    :重新修改工具配置;
  endif
endif

stop
@enduml